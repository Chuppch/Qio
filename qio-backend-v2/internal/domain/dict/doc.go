// Package dict 承载全站字典数据：可选头像、国家。
//
// 这些数据纯静态、无业务规则，但被多个业务域共用：
//
//   - 头像：user 域在修改资料时校验 avatarId 有效性
//   - 国家：user 域用于地址簿，letter 域在寄往境外时取首都坐标计算投递距离
//
// 因此单独成域而不归属任一业务域。本域是叶子——不依赖任何其他域，只被依赖，
// 所以 user 与 letter 同时引用它不会构成循环。
package dict
