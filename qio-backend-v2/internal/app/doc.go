// Package app 存放跨域用例（Use Case）编排。
//
// 当一个业务动作需要协调多个域时（例如「发信」要同时涉及用户扣费、封面生成、
// 信件落库、好友关系判断、邮件通知），该动作不属于任何单一域，放在本包内编排。
//
// 约定：
//   - 一个用例一个文件，文件名即用例名（如 send_letter.go）
//   - 只做编排与事务边界控制，业务规则仍归各域 service
//   - 依赖各域 service 与 domain/port 接口，不直连数据库
//   - 不依赖 transport，入参出参用领域模型而非 DTO
package app
